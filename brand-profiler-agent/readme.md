# brand-profiler-agent

Takes a list of links as an input and outputs a brand profile.

```typescript
// output
type ProfileBrandOutput = {
  description: string;
  products: string[];
  industry: string;
};
```
