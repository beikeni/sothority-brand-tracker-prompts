# relevant-links-collector-agent

This agent does an initial scan of a brand website and collects all relevant links, like blogs, social media profiles, and anything that could be considered relevant when building a brand profile.

```typescript
// output
type CollectRelevantLinksOutput = {
  links: string[];
};
```
